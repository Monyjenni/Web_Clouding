import AppLayout from "../AppLayout";

const HomePage = () => {
    return (
        <AppLayout title="Clean Blog" shortDec="A Blog Theme by Start Bootstrap">
            Home page
        </AppLayout>
    );
};

export default HomePage;